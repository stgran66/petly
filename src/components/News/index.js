import ArticleItem from './Article';
import NewsList from './NewsList';
import ArticleContainer from './ArticleContainer';

const newsComponents = { ArticleItem, NewsList, ArticleContainer };

export default newsComponents;